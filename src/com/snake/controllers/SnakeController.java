package com.snake.controllers;

import com.snake.handlers.BaseHandler;

public class SnakeController extends BaseController {
    public SnakeController(BaseHandler handler) {
        super(handler);
    }
}
