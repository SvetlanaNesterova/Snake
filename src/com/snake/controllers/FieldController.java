package com.snake.controllers;

import com.snake.handlers.BaseHandler;

public class FieldController extends BaseController {
    public FieldController(BaseHandler handler) {
        super(handler);
    }
}
